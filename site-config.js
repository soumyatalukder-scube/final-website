/*
 * Runtime website configuration.
 *
 * Update contactEmail before building or edit site-config.js directly in the
 * deployed files. The first FormSubmit submission will send a confirmation
 * message to this address. Automatic delivery starts after confirmation.
 */
window.SCUBE_CONFIG = {
  contactEmail: "contact@scubeaiconsulting.com",
  formEndpoint: "",
};
